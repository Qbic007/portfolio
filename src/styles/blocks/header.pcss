.header {
    display: flex;
    justify-content: space-between;
    width: 100%;
}

.header__logo {
    font-size: 36px;
    line-height: 30px;
    color: white;
    font-weight: 800;    
}

.header__menu {
    @include tablets() {
        display: none;
    }
}