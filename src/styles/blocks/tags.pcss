.tags__list {
    display: flex;
    flex-wrap: wrap;
}

.tags__item {
    padding: 10px 20px;
    border-radius: 25px;
    background-color: #f3f5f9;
    margin-right: 15px;
    cursor: pointer;
    margin-bottom: 15px;
}