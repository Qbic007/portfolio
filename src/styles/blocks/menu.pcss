.menu {
    color: white;
}

.menu__list {
    display: flex;
    flex-wrap: wrap;
}

.menu__item {
    padding-left: 32px;
    padding-right: 32px;
    border-bottom: 2px solid transparent;

    &:hover {
        color: #ff9a00;
        border-bottom: 2px solid #ff9a00;
    }
}